<%= link_to "Back to Event", event_path(id: @log.event_id) %>

<div class="container">
    <h2>Log Attendance</h2>

    <%= form_for( @log ) do |f| %>
    <%= render partial: 'form', locals: {f:f} %>
    <%= f.submit "Attend Event", disable_with: 'Attending Event...' %>
    <% end %>
</div>